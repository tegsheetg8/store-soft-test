# ============================================
# POS System - Environment Variables
# ============================================
# Copy this file to .env and update with your actual values
# cp .env.example .env

# ============================================
# Server Configuration
# ============================================
# Port on which the NestJS application will run
PORT=3000

# Application environment (development, production, test)
# This affects database synchronization and logging
NODE_ENV=development

# ============================================
# Database Configuration (PostgreSQL)
# ============================================
# PostgreSQL database host
DB_HOST=localhost

# PostgreSQL database port
DB_PORT=5432

# PostgreSQL database username
DB_USERNAME=postgres

# PostgreSQL database password
DB_PASSWORD=postgres

# PostgreSQL database name
DB_DATABASE=pos_db

# ============================================
# JWT Authentication Configuration
# ============================================
# Secret key for signing JWT tokens
# IMPORTANT: Change this to a strong, random string in production!
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production

# JWT token expiration time
# Examples: '24h', '7d', '1h', '15m'
JWT_EXPIRES_IN=24h

# ============================================
# Notes
# ============================================
# - Never commit the .env file to version control
# - In production, use a secure secret management system
# - Ensure DB_PASSWORD and JWT_SECRET are strong and unique
# - For production, set NODE_ENV=production (disables auto-sync and enables optimizations)
